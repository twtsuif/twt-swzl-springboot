<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suif.mapper.AskMapper">
    <insert id="add">
        insert into tb_post_user (post_id,user_id) values (#{postId},#{userId})
    </insert>
</mapper>